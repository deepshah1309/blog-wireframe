const intialState = [];

const reducer = (posts = intialState, action) => {
  switch (action.type) {
    case "FETCH_POST":
      break;
    case "CREATE":
      break;
    default:
      break;
  }
};
